# Desenvolvimento local
DATABASE_URL=jdbc:postgresql://localhost:5432/duelo64
DATABASE_USERNAME=duelo64
DATABASE_PASSWORD=troque-esta-senha
SERVER_PORT=8080

# No Supabase, use a URL JDBC fornecida pelo painel.
# Nunca envie o arquivo .env real para o GitHub.
